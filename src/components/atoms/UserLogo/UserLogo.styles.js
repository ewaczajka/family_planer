import styled from "styled-components";

export const UserLogo = styled.div`
    width: 140px;
    height: 140px;
    border-radius: 50%;
    font-size: 60px;
    position: relative;
    background: ${({ color }) => color};
    position: relative;
    cursor: pointer;
`